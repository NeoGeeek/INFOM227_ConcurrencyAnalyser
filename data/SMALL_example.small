function inc(n) {
  i = 0;
  x = 0;
  while (i < n) {
    x = x + 1;
    i = i + 1;
  }
  return 0;
}

function main() {
  t = spawn inc(1000);
  u = spawn inc(1000);
  await t; await u;
  return x;   // With races: non-deterministic, typically < 2000
}
