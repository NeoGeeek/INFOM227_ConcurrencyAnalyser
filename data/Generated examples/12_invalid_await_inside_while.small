// Example 12 (INVALID): await inside while (should be rejected by project constraint)
function f() { a = a + 1; return a; }

function main() {
  i = 0;
  t = spawn f();
  while (i < 2) {
    await t;          // INVALID: await inside while
    i = i + 1;
  }
  return 0;
}
